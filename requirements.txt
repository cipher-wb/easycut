# Lyra · ASTRA —— 桌面外壳依赖（仅用于「原生窗口」体验与打包；核心后端仍纯标准库）
#
# 安装：  python -m pip install -r requirements.txt
# 说明：
#   - pywebview：把网页渲染成原生桌面窗口（Windows 走 WebView2，macOS 走系统 WebKit）。
#       不装也能用——server.py 会自动回退到 Edge/Chrome/Chromium 应用窗口或浏览器标签。
#   - pyinstaller：将来打包桌面安装包时才需要。
#
# 这是本项目唯一允许的 pip 依赖范围，且都只服务于「桌面外壳/打包」，
# 不进入剪辑/导出等核心逻辑（那些仍坚持 Python 标准库）。

pywebview>=5.0        # 原生窗口外壳（Windows: WebView2；macOS: WebKit）
# pyinstaller>=6.0     # 仅打包时取消注释
