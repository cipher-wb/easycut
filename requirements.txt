# 轻剪 EasyCut —— 桌面外壳依赖（仅用于「原生窗口」体验与打包；核心后端仍纯标准库）
#
# 安装：  python -m pip install -r requirements.txt
# 说明：
#   - pywebview：把网页渲染成原生桌面窗口（Windows 走自带的 WebView2 运行时）。
#       不装也能用——server.py 会自动回退到 Edge/Chrome 应用窗口或浏览器标签。
#   - pyinstaller：将来把整个程序打包成单个 .exe 时才需要（同事零依赖运行）。
#
# 这是本项目唯一允许的 pip 依赖范围，且都只服务于「桌面外壳/打包」，
# 不进入剪辑/导出等核心逻辑（那些仍坚持 Python 标准库）。

pywebview>=5.0        # 原生窗口外壳（Windows: WebView2 + pythonnet 自动随装）
# pyinstaller>=6.0     # 仅打包成 exe 时取消注释
