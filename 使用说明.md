# 简单剪辑 · 多轨非线性编辑器 — 使用说明（v2）

一个**零安装、可离线**的本地视频剪辑小工具：用浏览器界面编辑，用你电脑本机的 ffmpeg 导出。
适合给视频加中文说明、做画中画、简单的多轨剪辑与拼接。全部在本机运行，不上传、不联网。

---

## 一、启动与停止

1. **双击 `启动.bat`** —— 会弹出一个黑色命令行窗口，并自动打开浏览器进入编辑界面。
2. **使用期间请勿关闭那个黑色窗口**：它就是后台服务，关掉它工具就停了。
3. 用完后，**直接关闭黑色窗口**即可停止服务。
4. 地址固定在 `http://127.0.0.1:8765`（若端口被占用会自动换一个，以黑窗里显示的网址为准）。浏览器没自动打开时，手动访问该网址即可。

> ⚠️ 如果点「导入」没反应、或页面打不开，多半是**后台服务没在运行**（黑窗被关了）。重新双击 `启动.bat` 即可。

---

## 二、界面速览

```
┌─────────────────────────────────────────────────────────────┐
│ 顶部工具栏：导入 / 添加文字 / 分割 / 删除 / 波纹删除 /          │
│             撤销 / 重做 / 播放 / 时间码 / + 轨道 / 导出         │
├──────────┬───────────────────────────────┬──────────────────┤
│ 左：素材库 │        中：预览画面             │ 右：属性 / 输出设置 │
│ (仓库区)  │   (选中的视频/文字带变换手柄)    │                  │
├──────────┴───────────────────────────────┴──────────────────┤
│ 下：多轨时间轴（刻度尺 + 播放头 + 多条轨道，每轨可静音/音量/锁/隐藏）│
└─────────────────────────────────────────────────────────────┘
```

- **素材库（左）**：导入的视频先进这里（带缩略图），不会自动上轨。
- **预览（中）**：实时显示当前画面；选中片段时可直接在画面上拖动、缩放。
- **属性 / 输出设置（右）**：选中对象后在这里精确调参数；底部是导出设置。
- **多轨时间轴（下）**：上面的轨道**盖住**下面的轨道（图层关系）。

---

## 三、基本流程（最短路径）

1. **导入** mp4 → 进素材库
2. 从素材库**拖**一段到下方某条轨道
3. **编辑**：剪切、加文字、做画中画
4. **预览**：按空格播放
5. **导出**：点右上「导出」，选保存位置

---

## 四、导入视频（两种方式）

| 方式 | 操作 | 特点 |
|------|------|------|
| **拖拽导入** | 从资源管理器把 mp4 拖进窗口 | 方便；会把文件**复制**一份到工作目录，大文件稍慢 |
| **选择导入** | 点工具栏「导入 mp4」或素材库「+ 导入」 | 直接用原文件、**不复制**，大文件更快 |

导入后视频出现在**素材库**。把素材库里的条目**拖到下方某条轨道的某个时间点**，即可生成一个片段。同一个素材可以拖到多条轨道、多个位置。

---

## 五、多轨道与图层

- 默认有 **2 条视频轨**。
- 点右上「**+ 轨道**」可新增 **视频轨** 或 **文字轨**。
- **上层轨道盖住下层轨道**（轨道越靠上 = 图层越靠前）。上层若是缩小的小窗（画中画），就会浮在下层画面之上；上层有空隙的地方，会露出下层。
- 每条轨道左侧的**轨道头**可以：
  - 🔊 **静音**该轨 / 拖动**音量**滑块
  - 👁 **隐藏**该轨（不参与画面）
  - 🔒 **锁定**该轨（防止误改）
  - 🗑 **删除**该轨

---

## 六、画中画（缩放 + 定位）

1. 在时间轴或预览里**选中一个视频片段**。
2. 预览画面上会出现**变换手柄**：
   - 拖**四角**：等比缩放（缩小即变成小窗）
   - 拖**画面内部**：移动位置
3. 也可在右侧**属性面板**精确设置：**位置、缩放、透明度**。
4. 把它放到上层轨道，就能做出**角标小窗 / 并排 / 画中画**效果。

---

## 七、文字标注

1. 点工具栏「**添加文字**」（会放到文字轨上）。
2. 在预览里拖动定位、拖角缩放；**双击**文字可直接改文案。
3. 右侧属性面板可调：**文案、字号、颜色、对齐、描边、半透明底框**，以及**出现时间 / 消失时间**。
4. 文字始终显示在所有画面之上。
   - 💡 提示：含 `¥` 等符号时，**字体建议用「微软雅黑」**（覆盖最全，避免方块）。

---

## 八、剪辑操作

| 操作 | 怎么做 |
|------|--------|
| **移动片段** | 直接拖动（可跨同类型轨道）。拖动时会**磁吸**到其它片段边缘 / 播放头 / 起点；**按住 Alt 临时关闭磁吸** |
| **修剪长度** | 拖动片段的**左右两端边缘**（不会超出原视频范围） |
| **分割** | 把播放头移到要切的位置，选中片段，按 **Ctrl+B**（或点「分割」） |
| **删除（留空隙）** | 选中片段按 **Delete**（或点「删除」） |
| **波纹删除（补缝）** | 选中片段按 **Backspace**（或点「波纹删除」）：删除后，**后面的片段自动前移补上空缺** |
| **复制 / 粘贴** | Ctrl+C 复制，Ctrl+V 粘到播放头处；Ctrl+D 原地复制一份 |
| **撤销 / 重做** | Ctrl+Z 撤销，Ctrl+Shift+Z（或 Ctrl+Y）重做 |

---

## 九、音频

- 导出时**所有轨道的声音会混合**在一起（多轨混音）。
- 每条视频轨可单独**静音**或**调音量**（轨道头）。
- 不想要任何声音：在右侧「输出设置」里取消勾选「**保留音轨**」。

---

## 十、播放与预览

- **空格**：播放 / 暂停
- 点击或拖动**刻度尺 / 播放头**：跳到任意时间
- **← / →**：后退 / 前进一帧；**Shift + ← / →**：后退 / 前进 1 秒
- **Home / End**：跳到开头 / 结尾
- 时间轴缩放：底部 **− / +** 按钮，或键盘 **+ / −**

> ⚠️ **关于预览卡顿**：当多条高分辨率视频轨同时播放时，浏览器内的预览可能**掉帧或在片段切换处瞬间黑一下**——这是浏览器同时解码多个视频的固有限制，属正常现象。
> **导出不受影响**：最终成片由 ffmpeg 离线精确合成，画质与同步都是准确的。预览只是「大致看效果」，**以导出结果为准**。

---

## 十一、完整快捷键表（剪映 / CapCut 风格）

| 快捷键 | 功能 |
|--------|------|
| `空格` | 播放 / 暂停 |
| `Ctrl + B` | 在播放头处分割选中片段 |
| `Delete` | 删除选中片段（留下空隙） |
| `Backspace` | 波纹删除（删除并使后面片段前移补缝） |
| `← / →` | 后退 / 前进一帧 |
| `Shift + ← / →` | 后退 / 前进 1 秒 |
| `↑ / ↓` | 选择上层 / 下层轨道的片段 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Shift + Z` 或 `Ctrl + Y` | 重做 |
| `Ctrl + C` / `Ctrl + V` | 复制片段 / 粘贴到播放头 |
| `Ctrl + D` | 原地复制一份片段 |
| `+` / `-` | 放大 / 缩小时间轴 |
| `Home` / `End` | 跳到开头 / 结尾 |
| `Ctrl + S` | 打开导出 |
| `Esc` | 取消选中 |

> 注：分割 / 删除 / 复制等需要**先选中一个片段**。
> 正在输入文字（光标在输入框里）时，只有 `Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y / Esc` 生效，其它按键交给正常打字。

---

## 十二、导出

1. 在右侧「**输出设置**」确认：
   - **分辨率预设**（或手动填**宽度 / 高度**）
   - **帧率 fps**
   - **画质 CRF**（数字越小越清晰、文件越大；默认 18 已是高画质）
   - **保留音轨**（多轨混音）是否勾选
2. 点右上「**导出**」（或 Ctrl+S）。
3. 选择保存位置和文件名。
4. 看进度条完成即可。

---

## 十三、常见问题（FAQ）

- **点「导入」没反应 / 页面打不开**：后台服务没运行。重新双击 `启动.bat`，并保持黑色窗口开启。
- **双击 `启动.bat` 后窗口里一堆"不是内部或外部命令"报错**：这是早期版本的编码问题，已修复（启动脚本现为纯英文）。若仍遇到，确认 `启动.bat` 与 `server.py` 在同一文件夹。
- **端口被占用**：会自动换端口，以黑色窗口里显示的网址为准。
- **拖拽导入很慢**：拖拽会复制整个文件，大视频请改用「选择导入」（用原文件、不复制）。
- **预览卡 / 切片段黑一下**：多视频预览的正常现象，导出是精确的，以成片为准。
- **中文 / 含空格的路径**：均支持。
- **某些字体显示方块**：换「微软雅黑」即可（覆盖字符最全）。
- **关掉浏览器标签了**：后台服务还在（黑窗没关），重新访问网址即可；编辑内容在网页里，**刷新或重开会丢失未导出的编辑**，重要的请及时导出。

---

## 十四、小贴士

- 先把所有素材导入素材库，再规划轨道，效率更高。
- 主画面放底层轨道，小窗 / 角标 / 文字放上层轨道。
- 多用磁吸对齐（默认开），需要自由摆放时按住 **Alt**。
- 随手 **Ctrl+Z** 不怕改错。
