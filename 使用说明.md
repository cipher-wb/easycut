# 简单剪辑 · 使用说明

一个本地网页版的简单视频剪辑工具。给视频加中文说明文字、做简单的剪切与拼接，然后导出成一个新的 MP4。

全部在你自己的电脑上运行，不上传、不联网、不需要安装任何东西。

---

## 一、它能做什么

1. **加中文说明文字**：在画面上拖出一个文字框，输入中文，调字号、颜色、位置，并设置这段文字"什么时候出现、什么时候消失"。
2. **剪切**：把一段视频从某个时间点切开、删掉不要的片段、拖动调整片段顺序。
3. **拼接**：再导入别的 MP4，把它的片段接到同一条时间轴上，连成一个视频。
4. **导出**：把以上所有操作合成为一个新的 MP4 文件，并显示导出进度。

适合：录屏演示、教学视频、操作步骤说明等"加字幕 + 简单裁剪"的场景。

---

## 二、怎么启动

1. 找到本目录下的 **`启动.bat`**，**双击**它。
2. 会弹出一个黑色的命令行窗口，显示正在启动；稍等一两秒，**浏览器会自动打开**编辑界面。
3. **使用期间请不要关闭那个黑色窗口**——它就是后台服务，关了工具就停了。
4. 用完后，直接**关闭那个黑色窗口**即可停止服务。

> 如果浏览器没有自动打开，看一下黑色窗口里提示的网址（形如 `http://127.0.0.1:8765`），手动复制到浏览器地址栏打开即可。
>
> 建议用 **Chrome 或 Edge** 打开，体验最好。

---

## 三、操作流程（推荐顺序）

### 第 1 步：导入视频

- 点顶部的 **"导入"** 按钮，会弹出系统的"打开文件"对话框，选择一个或多个 MP4。
- 选好后，每段视频会作为一个片段出现在**底部的时间轴**上。
- 多次导入会**依次追加**到时间轴后面（这就是"拼接"）。

### 第 2 步：剪切（裁剪 / 删除 / 排序）

时间轴在界面最下方，每个彩色块就是一个片段：

- **分割**：把播放头（时间轴上的竖线）拖到想切开的位置，点 **"分割"**，当前片段就在这里被切成两段。
- **删除**：点选一个片段（会高亮），点 **"删除片段"**，把不要的部分去掉。
- **调整顺序**：直接用鼠标**拖动**片段左右移动，改变它们的先后顺序。
- 时间轴上会显示每段时长和**总时长**。

> 小提示：太短的片段无法再分割（最短约一帧的长度），属于正常限制。

### 第 3 步：加中文说明文字

- 点顶部 **"添加文字"**，画面中央会出现一个文字框。
- **双击**文字框即可**输入/修改中文**内容（支持多行）。
- **拖动**文字框可移动位置；拖动**四角的小手柄**可改变框宽。
- 选中文字框后，**右侧属性面板**可以调：
  - **字号**（相对画面高度，预览即所见）
  - **颜色**、**不透明度**
  - **对齐**（左 / 中 / 右）
  - **描边**（开/关、颜色、粗细）——让白字在浅色背景上也看得清
  - **半透明底框**（开/关、颜色、透明度）——给文字加个背景条
  - **出现时间 / 消失时间**：这条文字只在这段时间里显示。
    - 可以直接填秒数，也可以把播放头移到某处，点 **"设为当前"** 把时间设成当前播放位置。
- 一条文字不要了，选中它点 **"删除文字"**。

> 所见即所得：你在预览里看到的文字位置、大小、颜色，导出后基本一致。

### 第 4 步：预览

- 点 **"播放/暂停"** 预览效果。播放头会**跨多个片段连续播放**，文字会按你设的出现/消失时间自动显隐。
- 拖动时间轴上的播放头（或进度条）可以跳到任意时间点查看。

### 第 5 步：导出

1. 点顶部 **"导出"**。
2. 可在导出设置里确认/调整：**分辨率、帧率、是否保留声音、画质（CRF，数字越小越清晰、文件越大，默认 18）**。
   - 分辨率默认取第一段视频的尺寸；不同尺寸的素材会自动缩放并居中加黑边，保证拼接正常。
3. 选择**保存位置和文件名**（系统"另存为"对话框）。
4. 开始导出后会显示**进度条**。导出是重新编码，时间长短取决于视频长度、分辨率和电脑性能，请耐心等待。
5. 完成后可点 **"打开所在文件夹"** 直接找到成片。

---

## 四、常见问题（FAQ）

**Q1：双击 `启动.bat` 后窗口一闪就没了 / 提示找不到 Python？**
说明电脑上没装 Python，或没加入 PATH。请到 <https://www.python.org/downloads/> 安装 **Python 3**，安装时勾选 **"Add Python to PATH"**，再重新双击 `启动.bat`。本工具只用 Python 自带的标准库，装好 Python 即可，无需 `pip install` 任何东西。

**Q2：提示端口被占用 / 8765 用不了？**
不用担心，程序会**自动换一个可用端口**并打开对应网址。一切以黑色窗口里显示的网址为准。

**Q3：导出失败，提示找不到 ffmpeg？**
本工具依赖 **ffmpeg**（含 ffprobe）。运行环境已确认 ffmpeg 在 PATH 中可用。若更换了电脑或重装系统导致找不到，请确保 `ffmpeg` 和 `ffprobe` 命令能在命令行直接运行（即已加入系统 PATH）。

**Q4：中文文字导出后变成方块 / 乱码？**
是字体问题。默认使用 **微软雅黑**（`C:\Windows\Fonts\msyh.ttc`）。可在文字属性的**字体下拉**里换成其它中文字体（黑体、宋体、楷体、等线等，下拉里只会列出你电脑上真实存在的字体）。只要选了中文字体，中文就能正常烧录到画面里。

**Q5：导出的视频没有声音？**
检查导出设置里的 **"保留声音"** 是否勾选。另外，如果原视频本身没有音轨，导出会补一段静音以保证时长正确。

**Q6：能撤销吗 / 关掉浏览器后还在吗？**
当前版本编辑状态保存在网页里，**刷新或关闭浏览器会丢失当前编辑**，请在满意后及时导出。导出的 MP4 文件会永久保存在你选择的位置。

**Q7：文字框拖出画面了会怎样？**
预览允许临时拖出边界，方便操作；**导出时会自动收回到画面范围内**，超出画面的部分会被自然裁掉，不会报错。

**Q8：可以处理含中文、空格的文件名/路径吗？**
可以。导入、导出、保存都支持中文和空格的路径。

---

## 五、一句话速记

> 双击 **`启动.bat`** → 浏览器自动打开 → **导入** MP4 → **分割/删除/拖动** 剪辑、**添加文字** 写说明 → **播放** 预览 → **导出** 成片。
>
> 用完直接关掉那个黑色命令行窗口即可。
