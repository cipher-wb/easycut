feat：定格 / 冻结帧（把某一帧拉长成静止画面）

新增"定格"：把视频某帧拉长成一段静止画面（讲解 + 文字说明给别人看），即剪映/CapCut
「定格」、Final Cut「Hold」式功能。定格片段仍是视频轨上的视频片段，freeze:true、
in==out==源帧时刻、duration 决定时间轴长度（与 in/out 解耦，仿文字片段），默认静音。

- 两个入口（顶栏「❄ 定格 ▾」下拉）：
  · 就地定格：在播放头切开当前片段，中间插入静止段，同轨后续右移（剪映式）。
  · 生成独立静止片段：单独生成一段静止画面，自由摆放 / 拉长。
  也可对 AI 说「把 X 在第 N 秒定格 M 秒」（ai.js 新增 freeze 指令）。
- app.js：clipDur / rebuildTimeline 加 freeze 分支；新增 createFreezeClip /
  freezeAtPlayhead / setFreezeProp；splitClip 支持 freeze 切半 + noHistory；
  trimClip 让 freeze 走 start/duration 路径；属性面板显示「时长 / 源帧时刻」。
- timeline.js：渲染 .freeze-clip 冷色 + ❄ 角标；拖边缘改时长（不裁源）；禁用 Shift 变速。
- player.js：syncVideos 把定格轨视频 seek 到源帧并暂停钉住（静止单帧）。
- ffmpeg_build.py：导出用 trim 抽一帧 + tpad=stop_mode=clone 保持 duration 秒、
  强制静音；无需新增 -i 输入、不影响 amix。
- index.html / style.css：定格下拉、属性面板字段、冷色样式（走 CSS 变量 + 日夜适配）。
- README / CLAUDE.md：新增「七·一 定格（冻结帧）」用法与数据模型 / 不变量说明。

验证：后端真跑 ffmpeg（freezedetect 检出静止段 start=2.0 / duration=3.0、输出总时长精确）；
Playwright 验证就地 / 独立 / 拖拽改时长 / 属性面板 / AI 指令 / 撤销，无报错。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
